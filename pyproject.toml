[project]
name = "dimpl"
version = "0.1.0"
requires-python = ">=3.13"

dependencies = [
    "mlflow>=3.9.0",
    "redis>=7.1.0",
    "rq>=2.6.1",
]

[tool.uv]
dev-dependencies = [
    "ruff",
    "ty",
]